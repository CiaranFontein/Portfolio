import React from "react";
import styled from "styled-components";

const StyledBody = styled.div`
  height: 10000px;
`;

const Body = () => {
  return <StyledBody>The Body</StyledBody>;
};

export default Body;
